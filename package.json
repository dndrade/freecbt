{
  "private": true,
  "workspaces": [
    "expo54",
    "www"
  ],
  "scripts": {
    "start": "run-pty run-pty.json",
    "start:expo": "yarn --cwd expo54 start",
    "build": "yarn --cwd expo54 build",
    "test": "yarn --cwd expo54 test",
    "format": "yarn --cwd expo54 format",
    "format:check": "yarn --cwd expo54 format:check"
  },
  "devDependencies": {
    "run-pty": "^5.0.0"
  },
  "resolutions": {
    "baseline-browser-mapping": "^2.11.15"
  }
}
