// Android's official "expanded" window size class starts at 840dp width.
// Shared by every adaptive layout primitive (ScreenContainer today, the
// planned Thoughts two-pane split later) so they don't drift onto different
// breakpoint numbers. Source: developer.android.com/develop/adaptive-apps/guides/use-window-size-classes
export const EXPANDED_WIDTH_BREAKPOINT = 840;
