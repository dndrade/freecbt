# view/gateways/

Components here import `model/` or `expo-router` and gate rendering or
navigation flow (onboarding, top-level providers).

Presentational components with no domain dependency are located in
`src/components/` (for example, `kv-table.tsx` and `image-path.ts`).
