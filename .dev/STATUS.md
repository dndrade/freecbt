# Data Compatibility Status

**Note (added 2026-08-07):** the encrypted-backup (`Archive-v3`) feature
work below the "Backup archive format"/"Backup file flow" rows is tracked
separately, not in this file — see
[`coordination/STATUS.md`](coordination/STATUS.md) (branch
`feat/encrypted-backup-archive-rebased`, PR #46, not yet merged to
`dndrade/main`). This file's `FAIL-013`/legacy-Save-target work below is
unrelated plaintext-backup history that predates and is independent of the
encrypted-archive feature.

**Archive note (2026-08-16):** the historical compatibility contract,
decision, and evidence trees now live under
[`../archive/`](../archive/). The
links below are retained as history unless they already point at the
coordination or investigation trees.

## Release Pair

| Side   | Release      | Commit                                     | State     |
| ------ | ------------ | ------------------------------------------ | --------- |
| Source | `2.4.0`      | `d024a02e8c05adfb86ff5fa3e6dbdb1369bcf002` | Confirmed |
| Target | `2.5.0-rc.1` | `d50d88c2a7eff66d80e1c1c2e8febc9e7870445e` | Confirmed |

## Headline Finding (2026-07-28, superseded 2026-07-29) — read this first

**Superseded.** An Android emulator device session on 2026-07-28 found the `2.5.0-rc.1` target build booting into a legacy UI by default (`(legacy)/index.tsx`'s redirect to `/v2` was commented out), with an ungated `(legacy)/debug.tsx` route and three broken backup mechanisms on that legacy screen. As of a 2026-07-29 source re-check, **all of that legacy code has since been removed from source**, and the finding no longer describes current behavior:

- The entire `(legacy)/**` route tree was deleted at `d77fbbb` (2026-07-24, "chore(cleanup): remove dead legacy UI, state, and utility tree"), with `src/legacy` renamed to `src/compat` and its `io-ts` code removed the same day (`0fe731d`, `2431b2a`).
- `expo54/src/app/index.tsx` now unconditionally redirects to `/v2` (`c4de6db`, "feat(routing): add root route redirecting to /v2", 2026-07-25 03:30 UTC).
- `v2/debug` remains gated behind `__DEV__` (approved `docs/superpowers/specs/2026-07-25-debug-route-gating-design.md`), and there is no other debug route left to gate.

The build tested on 2026-07-28 predated all four of those commits by three-plus days. **`v2` is not "the in-progress target" anymore — it is the only UI in current source, unconditionally reached on every launch.** Contracts `03`–`09`/`11`/`12` already describe `v2` as "current"; that was already correct and needs no re-pointing. `UNKNOWN-009` (routing) and `UNKNOWN-008` (debug gating) are both resolved — see `../archive/contract/14-unverified-assumptions.md`.

The device session's other findings remain historically valid **of the build tested**, not of current source: the three broken legacy backup mechanisms (`BACKUP-001-legacy-android-emulator.md`) and the `v2` "Save"-target partial defect (`BACKUP-002-v2-android-emulator.md`, still relevant — that's `v2` code, unchanged by the cleanup). Since there's no legacy screen left to fix, `BACKUP-001`'s defects are moot; `BACKUP-002`'s `v2` defect is not and remains open work under `10-backup-file-flow.md`.

Full detail: `investigations/2026-07-28-android-emulator-legacy-v2-backup/README.md` (six numbered findings, full repro commands, screenshots, and raw device logs) — kept as-is, a historical record of that build. Resolution notes added to `../archive/evidence/EVIDENCE-001-legacy-v2-routing.md`, `../archive/contract/02-compatibility-summary.md`, `../archive/contract/12-destructive-paths.md`, and `../archive/contract/14-unverified-assumptions.md`.

## Overall State

| Area                         | Contract                                             | Fixture    | Automated test | Device verification    | Release effect |
| ----------------------------- | ----------------------------------------------------- | ---------- | --------------- | ----------------------- | -------------- |
| Thought records                | Code-sourced (`v2` is current source; no legacy alternative remains) | Present    | Present          | Not run                 | Critical        |
| Thought key compatibility      | Code-sourced (`v2` is current source)                 | Present    | Present          | Not run                 | Critical        |
| Distortions                    | Code-sourced (`v2` is current source)                 | Present    | Present          | Not run                 | Critical        |
| Settings                       | Code-sourced; uncaught-decode risk fixed 2026-08-01 (PR #41), re-verified 2026-08-17 | Present | Verified 2026-08-17 | Not run                 | Critical        |
| PIN migration                  | Code-sourced; `Promise.all` ordering risk fixed 2026-08-01 (PR #41) | Present | Present    | Not run                 | Critical        |
| Backup archive format           | Code-sourced (format itself confirmed fine on device via `run-as`) | Present | Present     | Not run                 | Critical        |
| Backup file flow                | `v2`'s "Save"-target defect: code fix merged 2026-08-02 (PR #43, `9458cd1`), device re-verified 2026-08-02 — still open under a changed failure mode (`FAIL-013`); legacy defects moot (route deleted) | Missing    | Partial          | **Run — see BACKUP-003 (BACKUP-001/002 historical)** | Blocker |
| Reminders and notifications     | Code-sourced against `v2` (sole current UI)            | Unknown  | Partial          | Not run                 | High            |
| Destructive paths                | Code-sourced; `v2/debug` gated, no other debug route exists | Not needed | Partial          | Resolved — see Finding 6 (build predated cleanup) | Resolved |

## Completed Files

| Type     | File                                          | State                                 |
| -------- | ---------------------------------------------- | -------------------------------------- |
| Contract | `../archive/contract/00-scope-and-release-pair.md`        | Complete                                |
| Contract | `../archive/contract/01-evidence-rules.md`                | Complete                                |
| Contract | `../archive/contract/15-release-gates.md`                 | Complete                                |
| Contract | `../archive/contract/02-compatibility-summary.md`         | Code-sourced; routing caveat resolved (v2 is sole current UI) 2026-07-29 |
| Contract | `../archive/contract/03-storage-key-register.md`          | Code-sourced                            |
| Contract | `../archive/contract/04-thought-records.md`               | Code-sourced                            |
| Contract | `../archive/contract/05-thought-key-compatibility.md`     | Code-sourced                            |
| Contract | `../archive/contract/06-distortion-compatibility.md`      | Code-sourced                            |
| Contract | `../archive/contract/07-settings.md`                      | Code-sourced                            |
| Contract | `../archive/contract/08-pin-migration.md`                 | Code-sourced                            |
| Contract | `../archive/contract/09-backup-archive-format.md`         | Code-sourced                            |
| Contract | `../archive/contract/10-backup-file-flow.md`              | **Device-confirmed defect, root-caused** |
| Contract | `../archive/contract/11-reminders-and-notifications.md`   | Code-sourced (device portion open)      |
| Contract | `../archive/contract/12-destructive-paths.md`             | Code-sourced + ungated legacy route found, then resolved by deletion (2026-07-29) |
| Contract | `../archive/contract/13-failure-behavior.md`              | Code-sourced + 2 new device-confirmed failure modes (`FAIL-011`, `FAIL-012`) |
| Contract | `../archive/contract/14-unverified-assumptions.md`        | `UNKNOWN-008` reopened, then resolved 2026-07-29; `UNKNOWN-009` added and resolved |
| Evidence | `../archive/evidence/git-history-notes.md`                | Complete                                |
| Evidence | `../archive/evidence/current-source-map.md`               | Updated with routing-reachability caveat |
| Evidence | `../archive/evidence/EVIDENCE-001-legacy-v2-routing.md`   | Superseded 2026-07-29 — see resolution note in the file |
| Verification | `verification/results/backup/BACKUP-001-legacy-android-emulator.md` | New — FAIL |
| Verification | `verification/results/backup/BACKUP-002-v2-android-emulator.md` | Superseded by `BACKUP-003` — PARTIAL (historical) |
| Verification | `verification/results/backup/BACKUP-003-v2-android-emulator-fail-013-reverify.md` | New — FAIL, `FAIL-013` still open under a changed failure mode |
| Verification | `verification/results/backup/BACKUP-004-v2-android-emulator-fail-013-filename-reverify.md` | New — FAIL, filename-extension lead disproven |
| Investigation | `investigations/2026-07-28-android-emulator-legacy-v2-backup/README.md` | Full raw session log, 6 findings, 12 screenshots |
| Investigation | `investigations/2026-08-02-android-emulator-fail-013-save-reverify/README.md` | New — `FAIL-013` re-verification session log, screenshots, raw logs, screen recording |
| Investigation | `investigations/2026-08-02-android-emulator-fail-013-filename-reverify/README.md` | New — filename-extension lead re-test, screenshots, raw logs, screen recording |

"Code-sourced" means: exact keys, source paths, and historical-vs-current comparisons are cited and verified via `git show`/direct code reading against both the `expo-v2.4.0` tag and current `upstream/master`. It does not mean fixture, automated test, or device verification is complete — those remain separate columns above. The routing caveat that previously qualified "code-sourced" rows built against `v2` is resolved as of 2026-07-29 (see headline finding) — `v2` is current source, not a caveat.

**Note**: `.dev/` is gitignored (`.dev/.gitignore` has a bare `*`) — none of this contract work, including the previously completed 00/01/15, is tracked in git. This is being kept as intentional local-only workspace content per current direction; revisit if that changes.

## Release Gates

| Gate                          | State       | Evidence required                                                                                                   |
| ------------------------------ | ----------- | ---------------------------------------------------------------------------------------------------------------------- |
| A — Contract complete          | **BLOCKED** (on 1 genuine open item; 2 downgraded to accepted risk) | All 9 surfaces classified and sourced against `v2` (sole current UI); routing (`UNKNOWN-009`) and debug-gating (`UNKNOWN-008`) resolved 2026-07-29; app-ID identity confirmed matching via source (`UNKNOWN-001`, app-ID half). Signing-identity confirmation (Android + iOS) downgraded 2026-07-29 from hard blocker to an explicitly Inference-tier accepted-risk assumption (owner decision, dndrade) — rationale: store-enforced signing continuity for EAS-managed releases, plus Evan's known safety-first release discipline; backstopped by Gate C's device test catching any real mismatch as a clean install failure, not data loss. **Genuinely open**: the web-upgrade-scope decision (owner call, not evidence-gathering). See `../archive/contract/00-scope-and-release-pair.md`, `../archive/contract/14-unverified-assumptions.md` `UNKNOWN-001`. |
| B — Automated compatibility    | **Fixture/test matrix complete; `FAIL-013` remains open, both leads from the prior session now tried and one disproven; further debugging is owner-owned for now** | Historical fixtures and focused tests pass. Slice 1 (PIN `Promise.all` ordering `FAIL-004`/`FAIL-005`, settings `stringbool` decode `FAIL-003`) landed 2026-08-01, PR #41, merged to `dndrade/main` at `d4ee7d2`. Slice 2 (fixture/test matrix, `FIX-001`–`FIX-008`, `TEST-001`–`TEST-010`) landed 2026-08-01, PR #42, merged to `dndrade/main` at `2af56d2` (2026-08-01 21:00 EDT) — see "Gate B Slice 2 complete" below. `FAIL-013` MIME code fix merged 2026-08-02, PR #43, merged to `dndrade/main` at `9458cd1`; device re-run 2026-08-02 (`BACKUP-003-v2-android-emulator-fail-013-reverify.md`) found the "Save" target no longer appears in the share sheet at all rather than accepting the file — `FAIL-013` remains open under this changed failure mode. A second code fix (removing the `.txt` filename extension, branch `fix/fail-013-backup-filename-extension` @ `634808c`, PR #45) was device-tested the same day — see "FAIL-013 filename-extension re-verification" note below — and the lead is **disproven**: removing the extension did not restore the "Save" target. PR #45 merged to `dndrade/main` at `291d52a` 2026-08-02 23:04 UTC; local and remote `fix/fail-013-backup-filename-extension` branches deleted post-merge. Not Jest-testable. **The owner (dndrade) is taking further `FAIL-013` MIME/share-target debugging themselves for now** — see "Next Active Work" below for what else is available in the meantime. Remaining, unrelated to `FAIL-013`: `TEST-011` (build/static check, not Jest-testable). |
| C — Android in-place upgrade   | Not started | Recorded `2.4.0` -> `2.5.0-rc.1` device run against current `v2`-only source |
| D — iOS in-place upgrade       | Not started | Recorded `2.4.0` -> `2.5.0-rc.1` device run                                                                            |
| E — Backup round trip          | **FAIL on the "Save"-target sub-question (`v2`; legacy defects moot); full round trip now owner-confirmed working via Drive, with a UX gap noted** | `BACKUP-003` (`v2`, 2026-08-02 re-run) confirms the "Save"-target defect is still open, now under a changed failure mode (target absent from the share sheet rather than present-and-rejecting); `BACKUP-002` is superseded by it; `BACKUP-001` (legacy) is moot — that screen no longer exists. The Save-target sub-question is closed as a confirmed fail. **The round trip itself now has a positive result:** the owner exported to Drive, then imported that same file back into the app themselves, and confirmed the import reported success. This closes the gate's core functional question (export -> Drive -> import -> data restored works), though it was owner-run, not captured with the same evidence discipline (screenshots/logs/recording) as an agent-run device session. **New finding from that test:** the import result is only ever shown as plain, undifferentiated text — see "Gate E import UX finding" note below — worth a small follow-up UI change, not a correctness issue. |
| F — Full regression            | Not started | Existing release test plan completed                                                                                    |

## Next Active Work

**Start here next session (2026-08-02 end-of-session pointer, updated after PR #45 merge):** `FAIL-013`'s filename-extension lead is tried and disproven (see "FAIL-013 filename-extension re-verification" below); the fix still merged cleanly to `dndrade/main` at `291d52a` because it's a legitimate correctness improvement (filename/MIME agreement) independent of whether it closed the Android defect. **The owner is taking further `FAIL-013` MIME/share-target investigation themselves for now** — don't pick that specific thread back up unless asked. Other candidate next tasks, none blocked on `FAIL-013`, roughly in priority order:

1. **Gate E's import-success UI feedback.** The owner manually completed the full round trip (export -> Drive -> import) and confirmed it works, but the import result is shown as plain text with no visual distinction between success and failure (`expo54/src/app/v2/(public)/settings/backup.tsx:94`, `<Text style={[s.text]}>{importResult}</Text>` — same style regardless of whether `t("backup_screen.import.success")` or `t("backup_screen.import.file.failure")` is set). Add a success-styled treatment (e.g. green text, or a toast/popup) so success is unambiguous at a glance rather than reading like any other status line. Small, self-contained UI task — not device-only, doesn't need the emulator.
2. **Gate C** (Android in-place upgrade run: install `2.4.0`, load real data, upgrade to `2.5.0-rc.1`, confirm data survives) and **Gate D** (the same for iOS) — not started, no blockers, both need a device/simulator session same as the work above.
3. **Reminder native-schedule verification** (device-only half of `../archive/contract/11-reminders-and-notifications.md`) — not started; another device-session candidate that doesn't touch backup/share code at all.
4. **`TEST-011`** (a build/static check confirming a production build can't reach a debug route) — not device-only, but no such check exists in this repo yet; scoping what "build/static check" means here (a CI step? a script?) is itself part of the work.
5. **Gate A's one open item**: ask Evan (upstream owner) whether the EAS project/store account has changed since `2.4.0`, and get a web-upgrade-scope decision. Not blocking 1-4, and not something a session can resolve alone — it needs the owner to reach Evan.
6. **`FAIL-013`'s remaining untried lead** (re-testing `text/plain` on the current emulator/Play-services state to isolate MIME-driven vs. OS-drift target-set changes) is still here for the record, but per the owner's direction above, leave it for them to pick up rather than starting it proactively.

---

Gate A is parked in `BLOCKED` on 1 genuine open item (web-upgrade-scope decision); the two signing-identity items are downgraded to an accepted-risk assumption, not a local-investigation task — see Release Gates above. Work proceeds on the gates below in parallel rather than waiting on it.

1. ~~Decide fixture strategy: derive committed historical fixtures for thought records, settings, distortions, and the archive format from the shapes now documented in each contract file.~~ **Done 2026-08-02** — see "Gate B Slice 2 complete" below.
2. ~~Add the automated tests surfaced by this and the prior pass: PIN migration's `Promise.all` failure-ordering behavior (`FAIL-004`/`FAIL-005`) and settings' uncaught `z.stringbool()` decode (`FAIL-003`).~~ **Done 2026-08-01** — see "Gate B Slice 1 complete" below. `FAIL-013` remains excluded (see item 3).
3. Schedule remaining device-only work: Android/iOS in-place upgrade runs against current `v2`-only source, reminder native-schedule verification, and ~~a device re-run of `FAIL-013`'s "Save" target step now that its code fix is merged (`Sharing.shareAsync` in `download-or-share.tsx` — OS-level Android share-target rejection, not Jest-testable; this repo's Playwright setup is web-build-only, no device/e2e infra exists).~~ **Done 2026-08-02** — the "Save" target re-run happened; `FAIL-013` is confirmed **still open** under a changed failure mode (Save is no longer offered as a share target at all). See "FAIL-013 device re-verification" note below. Android/iOS in-place upgrade runs and reminder native-schedule verification remain not started.
4. If/when reachable, ask Evan (upstream owner, `erosson/freecbt`) the one narrow question — has the EAS project or store account changed since `2.4.0`? — and for a web-upgrade-scope decision. Tracked but not blocking items 1–3.

**Android device-testing method (decided 2026-08-02, logged 2026-08-02 20:00 EDT / America/New_York):** All Android-side device-only work in item 3 above — the `FAIL-013` `BACKUP-002` re-run, the Gate C in-place upgrade run, and reminder native-schedule verification — is to be run against an **Android emulator**, not a physical phone, reached the same way as the 2026-07-28 session that produced `BACKUP-001`/`BACKUP-002`: an ADB relay set up by the repository owner, connecting this environment's `adb` to the emulator. Owner rationale: the emulator path is more scriptable and gives the running agent full control to capture evidence, which a phone handed to a human tester would not.

**Evidence-capture requirement for future device-run sessions (same note):** whichever agent runs a device session going forward must capture, not just describe:
- **Screenshots** at each meaningful step (as the 2026-07-28 session already did — see `investigations/2026-07-28-android-emulator-legacy-v2-backup/screenshots/`, numbered sequentially per finding).
- **Raw logs**: `adb logcat` (both full and app-filtered/app-pid variants), relevant `adb shell dumpsys activity`/`dumpsys package` output, and any other command output cited as evidence — saved as files, not just pasted into the README (see `investigations/2026-07-28-android-emulator-legacy-v2-backup/raw-logs/` for the existing pattern and its own `README.md` index).
- **Screen recording**, new for this pass: capture video of the flow under test (e.g. `adb shell screenrecord /sdcard/<name>.mp4` for the duration of the interaction, then `adb pull` it into the investigation folder's `raw-logs/` or a new `recordings/` subfolder) wherever a still screenshot can't fully capture the behavior — the `FAIL-013` "Save" target flow (tap Save → toast appears → dismiss) is exactly this kind of case, since the toast's *timing and disappearance* is easy to miss in a single screenshot.

Follow the existing `investigations/<date>-<slug>/README.md` + `screenshots/` + `raw-logs/` structure for the new session's folder; add `recordings/` alongside them if video is captured. Update the relevant `verification/results/backup/BACKUP-002-v2-android-emulator.md` (or a new dated result file, if re-running rather than amending) and this STATUS.md file the same way the 2026-07-28 session's findings were distilled out to the contract files.

**Where we're leaving off (2026-08-01 21:01 EDT / America/New_York):** Both Gate B slices are merged to `dndrade/main` (Slice 1 at `d4ee7d2` via PR #41, Slice 2 at `2af56d2` via PR #42). Items 1 and 2 in Next Active Work are done. Nothing is currently blocked or mid-flight. The next open thread for a future session is **item 3 — scheduling device-only work** (not started):

- Android in-place upgrade run (`2.4.0` → `2.5.0-rc.1`) against current `v2`-only source — Gate C evidence.
- iOS in-place upgrade run — Gate D evidence.
- Reminder native-schedule verification (device-only half of `../archive/contract/11-reminders-and-notifications.md`).
- `FAIL-013` fix: **code merged 2026-08-02, PR #43, `dndrade/main` at `9458cd1`** (`expo54/src/platform/sharing/backup-mime.ts`, `expo54/src/app/v2/(public)/settings/backup.tsx`), changing the Android share MIME type from `text/plain` to `application/octet-stream`. **Still needs a device-verified re-run of `BACKUP-002`'s "Save" target step** — this was a hypothesis-driven fix, not a confirmed one; not Jest-testable, this repo's Playwright setup is web-build-only, no device/e2e infra exists.
- `TEST-011` (production build can't reach a debug route that clears data) — needs a build/static check, not Jest; no such check exists in this repo yet.

None of the above can proceed without emulator/device or simulator access — confirm that's available before starting a device-run session, otherwise item 4 below (the one open Gate A question for Evan) is the only other unblocked thread. Gate A stays `BLOCKED` on the web-upgrade-scope decision (owner call); the two signing-identity items are already downgraded to accepted risk, not action items.

**Gate B Slice 1 complete (2026-08-01):** Both risks flagged in this pass — `FAIL-004`/`FAIL-005` (PIN migration `Promise.all` ordering, `../archive/contract/08-pin-migration.md`) and `FAIL-003` (settings uncaught `stringbool` decode, `../archive/contract/07-settings.md`) — are fixed in source, each with a new failing-then-passing unit test. Merged to `dndrade/main` at `d4ee7d2` via PR #41. Full suite green (11 suites / 48 tests, up from the 46-test baseline). `FAIL-013` was scoped out of this slice from the start (see item 3) and is unaffected. Contracts `07`/`08` still read "Code-sourced" for the identification of the risk — that label describes the original discovery, not the fix; the fix itself is recorded in the Overall State table above and in this note, not by rewriting the contract files. Re-verified 2026-08-17 by the focused Settings UI and SettingsScreen Jest sweep.

**Gate B Slice 2 complete (2026-08-02, logged 2026-08-01 20:58 EDT / America/New_York):** Audited every existing test file against `verification/fixture-plan.md`'s `FIX-001`–`FIX-008` and `automated-test-matrix.md`'s `TEST-001`–`TEST-010` before adding anything new — found `FIX-001`/`FIX-002`/`FIX-005`/`FIX-008` (and their tests) already satisfied by pre-existing tests, some with stronger provenance than a freshly written fixture would have (`FIX-005`'s archive snapshots are real historical-format captures, not reconstructions). Added 3 new tests and one assertion to close the genuine gaps: `FIX-004`/`TEST-004` (malformed thought is non-destructive at the storage layer — `Storage.thoughts(...)` had zero prior test coverage), `TEST-006` (archive restore round-trips into storage, reusing the existing real snapshot), `FIX-003`/`TEST-003` (key-recovery regenerates the identical canonical key, extending an existing test), and `FIX-006`/`TEST-007` (historical `existingUser: "true"` literal — closes `UNKNOWN-003b`'s executed-confirmation gap). Along the way, `FIX-004`'s test also caught a real bug that `../archive/contract/04-thought-records.md`'s prior 'Code-confirmed' claim missed: `readAll()` actually threw on syntactically-invalid JSON in a single thought record instead of partitioning it gracefully — fixed in the same pass (commit `012bc66`). See `docs/superpowers/plans/2026-08-02-gate-b-fixture-strategy.md` (local-only, not committed) and its design doc. Full suite green (11 suites / 51 tests, up from the 48-test post-Slice-1 baseline). `TEST-011` and `FAIL-013` remain excluded — both need a build/static or device check, not Jest. Merged to `dndrade/main` via PR #42 at `2af56d2` (2026-08-01 21:00 EDT / America/New_York). Local and remote `docs/gate-b-fixture-strategy` branches deleted post-merge.

**Resolved 2026-07-29:** `UNKNOWN-003b` (does `z.stringbool().decode("true")` succeed?) — yes, confirmed by direct runtime check against `expo54`'s actual zod dependency. See `../archive/contract/14-unverified-assumptions.md`.

**Correction (2026-07-29):** an earlier pass on this date mislabeled `FAIL-012` as `v2`'s Save-target defect. Re-checked against `../archive/contract/13-failure-behavior.md`: `FAIL-010`, `FAIL-011`, and `FAIL-012` are all legacy-screen findings (from `BACKUP-001`), all moot now that `(legacy)/**` is deleted. The real, still-open `v2` Save-target defect (from `BACKUP-002`) had no `FAIL-` ID at all until this correction — it's now `FAIL-013`.

**Note (2026-07-29):** two items formerly listed here are resolved and removed:
- The debug-route gap (`(legacy)/debug.tsx`, no `__DEV__` gate) — the whole `(legacy)/**` tree was deleted from source at `d77fbbb` (2026-07-24), before the 2026-07-28 device session that found it ungated. See `../archive/contract/14-unverified-assumptions.md` (`UNKNOWN-008`).
- The legacy-vs-v2 re-pointing decision — moot, since `(legacy)/**` no longer exists in source at all (deleted alongside the above, `index.tsx` unconditionally redirects to `/v2` as of `c4de6db`, 2026-07-25). Contracts `03`–`09`/`11`/`12` already describe `v2` as current; nothing to re-point. See `../archive/contract/14-unverified-assumptions.md` (`UNKNOWN-009`).

**FAIL-013 code fix merged (2026-08-02, logged 2026-08-02 19:20 EDT / America/New_York):** The `2026-08-02-fail-013-backup-save-target-mime-fix` plan's code fix (`expo54/src/platform/sharing/backup-mime.ts`, `expo54/src/app/v2/(public)/settings/backup.tsx`) merged to `dndrade/main` at `9458cd1` via PR #43. Changes the backup archive's declared Android share MIME type from `text/plain` to `application/octet-stream` (import accepts both, for backward compatibility with files exported before this fix), a hypothesis-driven attempt to stop Android's built-in "Save" share target from rejecting the file. Full test suite green (12 suites / 53 tests, up from the 51-test post-Slice-2 baseline). Local branch `fix/fail-013-backup-save-target-mime` and worktree deleted post-merge; remote branch auto-deleted by GitHub on merge. `FAIL-013` remains open pending device confirmation — a re-run of `BACKUP-002`'s "Save" target step is still required; this was never Jest-testable, only the constant wiring is (`backup-mime.test.ts`). Not a claim that the underlying Android bug is resolved.

**FAIL-013 device re-verification (2026-08-02, logged 2026-08-02 17:40 EDT / America/New_York):** Re-ran `BACKUP-002`'s "Save" target step against the PR #43 build (`dndrade/main` at `9458cd1`) on the same emulator profile, with screen recording this time. Result: **`FAIL-013` is still open, under a changed failure mode.** The built-in "Save" target no longer appears in the share sheet at all — the sheet now lists only Quick Share, Drive, and Gmail — confirmed twice (screenshot and a fresh `uiautomator dump` after reopening the sheet). Android's built-in Save chip appears to be MIME-eligibility-gated: offered for `text/plain` (where it previously showed and rejected the file with a toast), not offered for `application/octet-stream`. The fix traded one failure mode for another rather than closing the gap. The remaining targets still accept the payload structurally, with no explicit rejection: Quick Share proceeded into its own first-run setup flow, and Drive proceeded into its own sign-in flow (same as `BACKUP-002`, still blocked only by no test account on this emulator). Full detail, screenshots, raw logs (`full-logcat.txt`, `app-filtered-logcat.txt`, `topresumed-after-share-tap.txt`, two `uiautomator dump` captures), and a screen recording of the Quick Share/Drive attempts: `investigations/2026-08-02-android-emulator-fail-013-save-reverify/README.md`. Distilled into `verification/results/backup/BACKUP-003-v2-android-emulator-fail-013-reverify.md` (`FAIL`), `../archive/contract/10-backup-file-flow.md`, and `../archive/contract/13-failure-behavior.md`'s `FAIL-013` entry. Gate E's Save-target sub-question is now closed as a confirmed fail (not an open question) — the gate's full round trip, including import, remains open regardless. **Still open for a future session:** the Android in-place upgrade run (Gate C), the iOS in-place upgrade run (Gate D), reminder native-schedule verification, and the full backup round-trip import test (Gate E) — none of these were exercised or newly informed by this re-run. No decision has yet been made on how to actually close `FAIL-013` (e.g., a custom share-target chooser, a different MIME value, or documenting the limitation and moving on) — that remains an open question for whoever picks this back up.

**Post-session addendum (2026-08-02, same day):** after this re-run concluded, the repository owner manually added a real Google account to the emulator (outside the automated session's reach) and completed the Drive upload that the session itself could only reach the sign-in prompt for. The upload succeeded, and the owner confirmed the resulting file's contents are intact and well-formed — a `:FreeCBT:`-wrapped export string consistent with the app's real archive format. This doesn't change `FAIL-013`'s result (still open — "Save" itself is still gone), but it's a genuine positive data point for Gate E: it confirms the underlying export payload and the Drive share pathway work end-to-end (export -> real destination -> file intact), narrowing the `FAIL-013` regression specifically to the "Save" target's disappearance rather than a broader breakage of share-based export. Import back into the app from that file was not attempted and remains open.

**FAIL-013 filename-extension re-verification (2026-08-02, logged 2026-08-02 18:58 EDT / America/New_York):** Device-tested the filename-extension lead recorded at the end of the prior session — that `FreeCBT-backup.txt`'s `.txt` extension, disagreeing with the `application/octet-stream` MIME type, might be gating the "Save" target. The code fix (branch `fix/fail-013-backup-filename-extension` @ `634808c`, PR #45) removes the extension, exporting as `FreeCBT-backup`. PR #45 merged to `dndrade/main` at `291d52a` on 2026-08-02, 23:04 UTC — the filename change stands on its own merits (the exported filename and its declared MIME type no longer disagree) independent of whether it closed the Android defect; local and remote `fix/fail-013-backup-filename-extension` branches deleted post-merge. Tested against the same emulator via the owner's ADB relay, with Metro serving live from that exact branch/commit. First attempt hit stale JS cached in the already-running app instance (share sheet still showed `.txt`); force-stopped and relaunched the app to pick up the new bundle, confirmed via screenshot that the filename was genuinely `FreeCBT-backup` this time. Result: **the lead is disproven.** The share sheet's target list — My Drive, Quick Share, Drive, Gmail — is unchanged from `BACKUP-003`'s post-MIME-fix result; "Save" is still absent, extension or no extension. Confirmed via screenshot, a fresh `uiautomator dump`, and a screen recording; a swipe across the icon row surfaced no hidden targets, and swiping up on the sheet dismissed it rather than expanding it, ruling out a truncated/scrollable list. Full detail: `investigations/2026-08-02-android-emulator-fail-013-filename-reverify/README.md`. Distilled into `verification/results/backup/BACKUP-004-v2-android-emulator-fail-013-filename-reverify.md` (`FAIL`). Of the two leads STATUS.md listed as untried at the end of the prior session, this is the first resolved (disproven); the `text/plain` re-test lead remains untried. **No decision has yet been made on how to actually close `FAIL-013`** now that the filename lead is exhausted — the remaining options (the untried `text/plain` re-test, a custom share-target chooser, a different MIME value, or documenting the limitation and moving on) are unchanged from the prior session's list. **The owner has said they may pick this specific investigation up themselves** — see "Next Active Work" above for other candidate tasks in the meantime.

**Gate E import UX finding (2026-08-02, logged 2026-08-02 19:12 EDT / America/New_York):** After PR #45 merged, the owner independently completed the full Gate E round trip by hand — exported to Drive (from an earlier session, file already confirmed intact), then imported that same file back into the app on their own device/session. The app reported the import succeeded. This is the first fully-confirmed export -> real destination -> import -> data-restored result for Gate E; it was owner-run, not captured with device-session evidence discipline (no screenshots/logs/recording), so it's recorded here as an owner-verified data point rather than a formal `verification/results/` entry. **While doing this, the owner noted a UX gap worth fixing regardless of `FAIL-013`:** the import result message renders as plain, undifferentiated text (`expo54/src/app/v2/(public)/settings/backup.tsx:94`) — success and failure look the same at a glance, both just default-styled `<Text>`. Filed as Next Active Work item 1 (success-styled feedback — green text, a toast, or similar — so success is visually unambiguous). Not a data-safety or correctness issue; the import itself works, only the feedback is unclear.
